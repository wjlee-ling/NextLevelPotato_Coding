def solution(stones, k):
    # # first try -> fails efficiency only
    # answer = 0
    # diffs = []
    # min_diff = 200000000
    # for idx in range(len(stones)-k+1):
    #     window = stones[idx:idx+k]
    #     diff = max(window)-min(window)
    #     diffs.append(max(window))
    # answer = min(diffs)    
    return answer
